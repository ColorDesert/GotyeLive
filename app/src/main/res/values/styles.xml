<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@android:color/black</item>-->
        <item name="colorPrimaryDark">@android:color/black</item>
        <!--<item name="colorAccent">@android:color/black</item>-->

        <!-- 窗口的背景颜色 -->
        <!--<item name="android:clipToPadding">true</item>-->
        <!--<item name="android:fitsSystemWindows">true</item>-->
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="popwin_anim_style">
        <item name="android:windowEnterAnimation">@animator/chat_show</item>/
        <item name="android:windowExitAnimation">@animator/chat_disappear</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--<style name="mypopwindow_anim_style">-->
        <!--<item name="android:windowEnterAnimation">@anim/popshow_anim</item>        &lt;!&ndash; 指定显示的动画xml  &ndash;&gt;-->
        <!--<item name="android:windowExitAnimation">@anim/pophidden_anim</item>       &lt;!&ndash; 指定消失的动画xml  &ndash;&gt;-->
    <!--</style>-->
</resources>